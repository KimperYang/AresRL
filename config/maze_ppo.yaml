hydra:
  searchpath:
    - pkg://verl/trainer/config

defaults:
  - ppo_trainer
  - _self_

trainer:
  project_name: maze_ppo
  experiment_name: qwen2_1_5b_maze
  logger: ["console"]
  total_epochs: 1
  test_freq: -1
  save_freq: 1
  default_local_dir: /mnt/tmp/training_res/rl
  nnodes: 1
  n_gpus_per_node: 1

data:
  train_files: data/maze/train.parquet
  val_files: data/maze/val.parquet
  train_batch_size: 2
  val_batch_size: 1
  max_prompt_length: 512
  max_response_length: 256
  return_raw_chat: True
  filter_overlong_prompts: False
  shuffle: True

actor_rollout_ref:
  model:
    path: Qwen/Qwen2-1.5B-Instruct
    trust_remote_code: False
  rollout:
    name: vllm
    load_format: auto
    tensor_model_parallel_size: 1
    data_parallel_size: 1
    pipeline_model_parallel_size: 1
    log_prob_micro_batch_size_per_gpu: 1
    prompt_length: 512
    response_length: 256
    max_model_len: 768
    do_sample: True
    multi_turn:
      enable: True
      max_assistant_turns: 28
      max_user_turns: 28
      interaction_config_path: config/maze_interaction.yaml
    agent:
      num_workers: 1
      default_agent_loop: maze_agent
      agent_loop_config_path: config/maze_agent_loop.yaml
  actor:
    ppo_mini_batch_size: 2
    ppo_micro_batch_size_per_gpu: 1

critic:
  model:
    path: Qwen/Qwen2-1.5B-Instruct
  ppo_micro_batch_size_per_gpu: 1

reward_model:
  enable: False
